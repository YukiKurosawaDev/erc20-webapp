import { Button, Text, ArrowDownIcon, Box, useModal } from '@pancakeswap-libs/uikit'

const Home: React.FC = () => {
    return (
        <div>
            <div>TEST OK!</div>
            <Button>AAA</Button>
        </div>
    )
};

export default Home;